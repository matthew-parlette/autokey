AutoKey should be installed via salt, this repo should be cloned to ~/.config after removing ~/.config/autokey.

If you don't remove ~/.config/autokey first, you'll keep the templates that come with the program.
